# Base URL of the deployed backend API, including /api.
# In local dev this can be left unset — Vite proxies /api to localhost:4000 (see vite.config.js).
VITE_API_BASE_URL=https://your-backend.onrender.com/api
